import { LINKEDIN_URL } from "@/helpers/auth";

const Hello = () => {
    return (
        <div>
            <button><a href={LINKEDIN_URL}>Linkedin Login</a></button>            
        </div>
    )
}

export default Hello;
